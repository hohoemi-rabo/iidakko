# 08 — 静的データ整備（記事・施設・イベント・緊急＋ジオコーディング）

Phase 1 はデータをすべて静的な TypeScript で持つ。施設座標はビルド前に一度だけジオコーディングして数値で直書きする（実行時API問い合わせをしない）。

## 参照
- `REQUIREMENTS.md` 6章（データ構造・サンプル）・3章（地図データの作り方）・0章（やらないこと：実行時ジオコーディング禁止）

## 依存
- 01（型定義 `types.ts`）

## Todo

### データ投入（各カテゴリ代表数件で十分／出典は飯田市公式）
- [×] `data/articles.ts`：授乳・育児相談、児童手当、産後ケア、乳幼児健診、入園手続き 等 7件（`Article` 型、URLは公式実在確認済み）
- [×] `data/facilities.ts`：保育園5・認定こども園2・つどいの広場5・こども食堂1 の計13件（`Facility` 型、住所は公式確認済み）
- [×] `data/events.ts`：マタニティサロン(6/6)・BP1赤ちゃんがきた!(7月) の2件（`EventItem` 型）※おもちゃばこ通信の講座は月次PDF内で日付抽出できず、わくわく並木広場は2026年日付が公式未掲載のため未掲載。後日手動追加可。
- [×] `data/emergency.ts`：♯8000・休日夜間急患診療所・症状別PDF の3件
- [×] すべてのレコードに公式 `url`（出典）を持たせる

### ジオコーディング（ビルド前に一度だけ）
- [×] `scripts/geocode.mjs` を用意（住所→緯度経度、Geocoding API 使用。env からキー読込・住所キャッシュ・lat/lng 直書き・失敗時は0据置）
- [×] `GOOGLE_MAPS_API_KEY=... node scripts/geocode.mjs` で施設座標を取得（13件すべて成功・失敗0）
- [×] 取得した `lat` / `lng` を `facilities.ts` に数値で直書き（スクリプトが自動で書き込み済み）
- [×] APIキーは環境変数で渡し、コミットしない（`.env` は gitignore 済みを確認）

## 完了条件
- [×] 各 `data/*.ts` が型に適合し、画面から import できる（tsc 通過）
- [×] 施設に正確な座標が直書きされている（13件すべて飯田市域の座標で直書き済み）
- [×] 実行時にジオコーディングAPIを呼ばない構成になっている（座標は静的直書き設計、実行時API呼び出しなし）
- [×] 全レコードが公式URLへ遷移可能
