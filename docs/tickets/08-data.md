# 08 — 静的データ整備（記事・施設・イベント・緊急＋ジオコーディング）

Phase 1 はデータをすべて静的な TypeScript で持つ。施設座標はビルド前に一度だけジオコーディングして数値で直書きする（実行時API問い合わせをしない）。

## 参照
- `REQUIREMENTS.md` 6章（データ構造・サンプル）・3章（地図データの作り方）・0章（やらないこと：実行時ジオコーディング禁止）

## 依存
- 01（型定義 `types.ts`）

## Todo

### データ投入（各カテゴリ代表数件で十分／出典は飯田市公式）
- [ ] `data/articles.ts`：授乳・育児相談、児童手当、産後ケア、休日/延長保育、乳幼児健診 など（`Article` 型）
- [ ] `data/facilities.ts`：保育園・認定こども園・一時預かり・つどいの広場・こども食堂・遊び場（`Facility` 型）
- [ ] `data/events.ts`：わくわく並木広場(6/14)・ゆいきっず講座(6/4)・親子フラ体験会・BP1 など（`EventItem` 型）
- [ ] `data/emergency.ts`：♯8000・休日夜間当番医・症状別PDF
- [ ] すべてのレコードに公式 `url`（出典）を持たせる

### ジオコーディング（ビルド前に一度だけ）
- [ ] `scripts/geocode.mjs` を用意（住所→緯度経度、Geocoding API 使用）
- [ ] `GOOGLE_MAPS_API_KEY=... node scripts/geocode.mjs` で施設座標を取得
- [ ] 取得した `lat` / `lng` を `facilities.ts` に数値で直書き
- [ ] APIキーは環境変数で渡し、コミットしない（`.env` は gitignore 済み）

## 完了条件
- [ ] 各 `data/*.ts` が型に適合し、画面から import できる
- [ ] 施設に正確な座標が直書きされている
- [ ] 実行時にジオコーディングAPIを呼ばない構成になっている
- [ ] 全レコードが公式URLへ遷移可能
