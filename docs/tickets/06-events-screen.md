# 06 — イベント画面（events）

地域の子育てイベントを日付順カードで集約。元PDF/SNSの情報を1つのデータモデルに整理して見やすく示す。

## 参照
- `REQUIREMENTS.md` 5章④イベント・6章（EventItem 型）
- `docs/ui-references/sagasu_ivent.png`（右半分：イベント・おでかけ）

## 依存
- 01（基盤）, 02（共通コンポーネント）, 08（イベントデータ）

## Todo
- [×] 日付順（`sortKey` 昇順）のカードリスト（今後＝昇順／終了＝末尾に降順）
- [×] `EventCard`：日付タグ・対象年齢タグ・カテゴリタグ＋場所＋申込/詳細リンク（既存部品を使用）
- [×] カテゴリのピル絞り込み（こども食堂 / 講座 / ひろば / その他）※汎用 `FilterChips` を新設し共用
- [×] 過ぎたイベントは非表示 または グレーアウト（`sortKey` と現在日で判定）→ グレーアウトして末尾に「終了したイベント」
- [×] `needsApply` のとき「要事前申込」を明示、`url` を `expo-linking` で開く（EventCard 内で実装済み）
- [×] 場所表記（テキスト）。`lat/lng` からのマップ連携は現データに座標が無く「必要なら」のため見送り

## 完了条件
- [×] イベントが日付順に並ぶ
- [×] カテゴリ絞り込みが動作する
- [×] 過去イベントが非表示/グレーアウトされる（グレーアウト＋末尾。現データに過去分は無いためロジックで担保）
- [×] 申込/詳細リンクが開く
